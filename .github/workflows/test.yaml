name: test action
on:
  push:
    branches:
      - master
      - dev
  pull_request:

jobs:
    test:
        runs-on: ubuntu-latest
        steps:
        - uses: actions/checkout@v3
        - uses: ./
          with:
            args: --help